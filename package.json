{
  "name": "desktop-pet-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev:desktop": "npm --workspace @desktop-pet/desktop run tauri dev",
    "build:desktop": "npm --workspace @desktop-pet/desktop run tauri build",
    "check:frontend": "npm --workspace @desktop-pet/desktop run check",
    "build:frontend": "npm --workspace @desktop-pet/desktop run build"
  },
  "engines": {
    "node": ">=20"
  }
}

